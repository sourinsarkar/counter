import CounterPage from "../components/CounterPage";

const Home: React.FC = () => {
  return (
    <div className="h-screen flex justify-center">
        <CounterPage />
    </div>
  );
};

export default Home
